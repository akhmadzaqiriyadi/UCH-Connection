# Production Environment Variables
# Copy this file to .env.production on VPS and update values

# ===========================================
# DATABASE (Remote VPS)
# ===========================================
DATABASE_URL=postgresql://uch_user:CHANGE_THIS_PASSWORD@10.10.10.100:2100/uch_connection_db

# ===========================================
# REDIS (Local on App Server)
# ===========================================
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ===========================================
# JWT SECRETS
# ===========================================
# IMPORTANT: Generate secure random strings for production!
# Use: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_64_CHAR_STRING
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=604800

# ===========================================
# APPLICATION
# ===========================================
PORT=2201
NODE_ENV=production

# ===========================================
# EMAIL (SMTP)
# ===========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="UTY Connection <no-reply@uty.ac.id>"

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. NEVER commit this file to Git
# 2. Use strong database passwords (min 16 chars)
# 3. Change JWT_SECRET to random string
# 4. Keep this file permissions: chmod 600 .env.production
